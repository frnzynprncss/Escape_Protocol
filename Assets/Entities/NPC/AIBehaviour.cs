using UnityEngine;

public class AIBehaviour : MonoBehaviour
{
    public Transform target;

    /*
        TODO:
            If npc is near the target, switch to chase State
            if npc is near attacking range of target, stop moving and switch to attack state
            if target is outside NPC Range, stop moving and switch to idle state
    */
}
